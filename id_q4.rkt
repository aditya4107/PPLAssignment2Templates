#lang racket

(define (print-insert-result x lst n)
  ;; write logic here

(define (main)
  (define x (read))
  (define lst (read))
  (define n (read))
  (print-insert-result x lst n))

(main)
